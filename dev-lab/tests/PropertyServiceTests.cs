using Connells.PropertyApi.Models;
using Connells.PropertyApi.Services;

namespace Connells.PropertyApi.Tests;

/// <summary>
/// Unit tests for PropertyService.
/// This file is intentionally empty — use Copilot to generate the tests!
/// 
/// Exercise 1: Place your cursor below and type "// Test" — let Copilot suggest tests
/// Exercise 2: Select the PropertyService class → right-click → Copilot → Generate Tests
/// Exercise 3: Use /tests in Copilot Chat to generate a full test suite
/// Exercise 4: Ask Copilot to add edge case tests (negative IDs, empty data, duplicates)
/// </summary>
public class PropertyServiceTests
{
    // TODO: Use Copilot to generate tests for:
    //   - GetAll returns all properties
    //   - GetById returns correct property
    //   - GetById returns null for non-existent ID        (this will reveal the bug!)
    //   - Add creates a new property with unique ID
    //   - Add sets ListedDate automatically
    //   - Update modifies all fields correctly
    //   - Update returns null for non-existent ID
    //   - Delete removes the property
    //   - Delete returns false for non-existent ID
}
